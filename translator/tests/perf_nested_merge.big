# Fihcier qui a permis la localisation de la perte memoire dans le cockpit, fonctionne normalement maintenant
atomic ctrl A = 1;
atomic ctrl B = 1;
atomic ctrl C = 1;
atomic ctrl D = 1;

big double_merge = (A{v} | B{w}) | (C{y} | D{z});
